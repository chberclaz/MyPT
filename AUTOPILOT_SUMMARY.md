# Autopilot summary

The unattended SFT loop (pack → rsync → train → eval gate → GOLD pull) is described in [docs/sft/AUTOPILOT.md](docs/sft/AUTOPILOT.md). Command-level runbook: [autopilot_agent.md](autopilot_agent.md).

Measured gate tables, winning mixes, and GPU-hours from this project's window are not published.
