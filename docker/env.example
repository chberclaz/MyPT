# =============================================================================
# MyPT Environment Configuration
# =============================================================================
# Copy this file to .env in the project root and customize as needed.
# Usage: cp docker/env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Web Application Settings
# -----------------------------------------------------------------------------

# Host to bind to (0.0.0.0 for network access, 127.0.0.1 for local only)
MYPT_HOST=0.0.0.0

# Port for the web application
MYPT_PORT=8000

# Enable debug mode (verbose logging of prompts, responses, tool calls)
MYPT_DEBUG=false

# JWT signing key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# IMPORTANT: Set this in production for persistent sessions!
# MYPT_SECRET_KEY=your-secure-secret-key-here

# -----------------------------------------------------------------------------
# GPU Configuration
# -----------------------------------------------------------------------------

# Which GPUs to use (comma-separated, or 'all')
# Examples: 0, 0,1, all
CUDA_VISIBLE_DEVICES=all

# -----------------------------------------------------------------------------
# Volume Paths (for bind mounts instead of named volumes)
# -----------------------------------------------------------------------------
# Uncomment and modify these to use local directories instead of Docker volumes
# Note: configs is seeded with defaults on first run if empty

# MYPT_CHECKPOINTS_PATH=./checkpoints
# MYPT_WORKSPACE_PATH=./workspace
# MYPT_DATA_PATH=./data
# MYPT_LOGS_PATH=./logs
# MYPT_CONFIGS_PATH=./configs

# -----------------------------------------------------------------------------
# Custom Model Configurations
# -----------------------------------------------------------------------------
# The configs volume contains model architecture presets (tiny, small, 150M, etc.)
# On first container start, default configs are copied to the volume.
# You can then customize these for your specific training needs.

