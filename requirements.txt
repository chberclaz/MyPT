# Core dependencies for MyPT
# A minimal GPT implementation with modular architecture

# PyTorch - Deep learning framework
# Install with CUDA support (recommended):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Or CPU only:
#   pip install torch torchvision torchaudio
torch>=2.0.0

# Tiktoken - OpenAI's tokenizer for GPT-2 BPE encoding
tiktoken>=0.5.0

# NumPy - Array operations for embeddings, data loading, shards
numpy>=1.21.0

# ============================================================================
# Web Application (Optional - for GUI)
# ============================================================================
# Install with: pip install -r requirements.txt
# Or: pip install mypt[webapp]

# FastAPI - Modern web framework
fastapi>=0.104.0

# Uvicorn - ASGI server
uvicorn>=0.24.0

# Jinja2 - Template engine
jinja2>=3.1.0

# File uploads
python-multipart>=0.0.6

# WebSockets for real-time updates
websockets>=12.0

# Authentication
passlib[bcrypt]>=1.7.4  # Password hashing with bcrypt
python-jose[cryptography]>=3.3.0  # JWT tokens (optional, has fallback)

# ============================================================================
# Optional: Development dependencies (for testing, linting, etc.)
# ============================================================================
# Uncomment to install:
# pytest>=7.0.0
# black>=23.0.0
# isort>=5.12.0
# flake8>=6.0.0
# mypy>=1.0.0

